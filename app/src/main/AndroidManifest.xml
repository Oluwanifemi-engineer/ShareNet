<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- The app relays packets on behalf of P2P clients, and reads the phone's
         own network state to describe the upstream connection. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Wi-Fi Direct (P2P) discovery on Android 13+ uses NEARBY_WIFI_DEVICES.
         Wi-Fi Direct discovery does not need location on 13+, so we declare
         neverForLocation and do NOT read location-derived data. -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    <!-- API 24–32 (Android 12 and below): P2P discovery requires fine location.
         COARSE must be declared alongside FINE (Android 12+ lint rule). -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="32" />

    <!-- Notifications + foreground service (type connectedDevice covers
         Wi-Fi Direct sessions; vpn covers the client tunnel; required on
         Android 14+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Android 16 (API 36) removed the public VPN FGS type/permission;
         the client tunnel declares specialUse instead. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <uses-feature
        android:name="android.hardware.wifi.direct"
        android:required="true" />

    <!-- A relay app should not leak the session host/PIN into cloud backups,
         and a restore could re-route traffic unexpectedly: no backup. -->
    <application
        android:name=".ShareNetApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShareNet">

        <!-- Sentry's auto-init provider would crash without a DSN; disable it
             and let ShareNetApp init the SDK only when a DSN is configured. -->
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ShareService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- 'vpn' is not a manifest-valid foregroundServiceType flag (the VPN
             type is set programmatically in startForeground), so the service
             declares specialUse with its required subtype property instead. -->
        <service
            android:name=".tunnel.TunnelVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="ShareNet Wi-Fi Direct client tunnel (UDP relay)" />
        </service>
    </application>

</manifest>
