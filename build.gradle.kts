// ShareNet — share your phone's Wi-Fi (or cellular) connection with other
// devices, no root, while staying connected to Wi-Fi yourself.
//
// Version pins match the Magneetar project's proven setup (Gradle 8.12,
// AGP 8.10.1, Kotlin 2.0.21) so the build reuses the local Gradle caches.
plugins {
    id("com.android.application") version "8.10.1" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
